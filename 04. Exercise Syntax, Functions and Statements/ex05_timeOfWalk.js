function timeOfWalk(steps, meters, km) {
    let distanceInMeters = steps * meters;
    let speedMetersInSecond = distanceInMeters / 3.6;
   
    
    console.log(`${}:${}:${}`);
}

timeOfWalk(4000, 0.60, 5);  // 00:32:48
//timeOfWalk(2564, 0.70, 5.5); // 00:22:35