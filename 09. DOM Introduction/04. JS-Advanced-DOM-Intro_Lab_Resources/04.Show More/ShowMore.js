function showText() {
    // TODO
    let hidden = document.getElementById("more");
	let textToShow = document.getElementById("text");

	textToShow.style.display = "inline";
	hidden.style.display = "none";
}

// if you run it you can't see anything in the console,
// because the essential changes are only visible in the browser
